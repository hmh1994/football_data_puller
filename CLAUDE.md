# CLAUDE.md

## Git Workflow

### Commits

- **NEVER create commits automatically**
- User will handle all git commits manually
- Only stage changes when explicitly requested
- Do not use `git commit` command unless explicitly asked

## Code Style

### `__init__.py`

- Keep `__init__.py` files empty
- No re-exports or `__all__` definitions